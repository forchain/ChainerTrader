## 1. 选币逻辑

- [ ] 1.1 确认 Binance 现货成交额榜单所用接口及字段，验证可筛选 `USDT` 现货交易对并按成交额排序
- [ ] 1.2 定义稳定币基础资产黑名单，至少覆盖 `USDT`、`USDC`、`BUSD`、`FDUSD`、`TUSD`、`USDP`、`DAI`
- [ ] 1.3 实现 Top 10 选币逻辑：筛选 Binance 现货 `USDT` 交易对、排除稳定币、按成交额排序取前 10
- [ ] 1.4 为选币逻辑增加测试，覆盖稳定币排除与 Top N 截断

## 2. 数据补齐与窗口读取

- [ ] 2.1 复用现有增量更新逻辑，为每个 Top 10 symbol 补齐 `1d` 最近 1 个月数据
- [ ] 2.2 复用现有增量更新逻辑，为每个 Top 10 symbol 补齐 `1h` 最近 1 周数据
- [ ] 2.3 实现数据库窗口读取层：按时间范围从 MongoDB 取出 `1d` 与 `1h` K 线
- [ ] 2.4 增加测试，覆盖“库中完整 / 前缺 / 后缺 / 数据不足”场景

## 3. 策略扫描与信号采集

- [ ] 3.1 设计策略扫描接口，支持通过参数指定策略名，默认 `macd_triple_divergence`
- [ ] 3.2 为默认策略接入最小信号采集适配层，只导出 `LONG` / `SHORT` 入场事件
- [ ] 3.3 对每个 `symbol + interval` 执行策略，收集窗口内全部命中的入场信号
- [ ] 3.4 增加测试，验证不输出 `CLOSE` / `STOP` / `TP` 事件

## 4. 结果汇总与 CLI

- [ ] 4.1 新增批量扫描脚本，串联“选币 → 补库 → 读库 → 跑策略 → 汇总”
- [ ] 4.2 实现结果全局排序，按 `signal_time` 输出全部命中的信号
- [ ] 4.3 提供终端表格输出，并支持可选 JSON 输出
- [ ] 4.4 增加 CLI 参数，至少包括 `--strategy`、`--top`、`--json-out`

## 5. 端到端验证

- [ ] 5.1 在开发环境执行一次默认扫描，验证可拿到 Top 10 非稳定币 `USDT` 交易对
- [ ] 5.2 验证数据库补齐后，策略执行阶段只从数据库读取窗口数据
- [ ] 5.3 验证输出只包含 `LONG` / `SHORT` 入场信号，且按时间全局排序
- [ ] 5.4 验证默认策略为 `macd_triple_divergence`，不传参数时行为符合预期


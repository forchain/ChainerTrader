## Archive note (2026-04-26)

This change is archived as **superseded by the current repository state**.

The repository layout policy and migration described in this change are already reflected in the live codebase and user-facing docs:

- Repository layout guidance is maintained in `AGENTS.md` and `CLAUDE.md`.
- Configuration assets live under `configs/` (tasks and notices), and README documents these locations.
- Repository layout reference exists at `docs/architecture/repository-layout.md`.

This change folder does not include a `.openspec.yaml` and its `tasks.md` checklist was not kept up to date during the transition to the current OpenSpec workflow, so it should not be treated as an active, executable change definition.

